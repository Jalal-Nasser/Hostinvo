# Hostinvo Docs

This folder is reserved for project documentation that grows alongside implementation phases.

- `archive/` keeps superseded or reference-only planning notes.
- Root-level master specs remain the authoritative input for now.
- `VERCEL_VPS_DEPLOYMENT.md` defines the production split between the Vercel frontend runtimes and the VPS/Plesk Laravel API runtime.
